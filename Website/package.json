{
  "name": "carbone-stellaire-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "npm run validate:content && astro build",
    "preview": "astro preview --host 0.0.0.0",
    "preview:local": "astro preview --host 127.0.0.1",
    "validate:content": "node scripts/validate-content.mjs"
  },
  "dependencies": {
    "astro": "^7.0.3",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
