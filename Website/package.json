{
  "name": "carbone-stellaire-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run sync-content && astro dev --host 0.0.0.0",
    "build": "npm run sync-content && npm run validate:catalog && astro build",
    "preview": "astro preview --host 0.0.0.0",
    "sync-content": "node scripts/sync-content.mjs",
    "validate:catalog": "node scripts/validate-catalog.mjs",
    "bootstrap:translations": "node scripts/bootstrap-product-translations.mjs"
  },
  "dependencies": {
    "astro": "^7.0.3",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
